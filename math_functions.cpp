#include<iostream>
#include <cmath>
using namespace std;


int main() {
    
    // int a = 2, b = 3;

    // double num = 4.5, p = 2;

    // // int d = pow(a,b);

    // double d = pow(num,p);

    // cout << d << endl;

    // double n = 27;

    // cout << cbrt(n) << endl;

    //cout << floor(4.9) <<endl; 

    // double n = 4.9;
    // cout << trunc(n) << endl;

    // int a = 3, b = 4;
    // int minimum = min(a,b);
    // cout << minimum << endl;
    // int maximum = max(a,b);
    // cout << maximum << endl;

    // int n = 7;

    // cout << __builtin_popcount(n) << endl;

}